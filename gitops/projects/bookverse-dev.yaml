apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: bookverse-dev
  namespace: argocd
spec:
  description: BookVerse DEV environment
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: '*'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

