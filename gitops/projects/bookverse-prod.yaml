apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: bookverse-prod
  namespace: argocd
spec:
  description: BookVerse PROD environment
  sourceRepos:
    - 'https://github.com/Rodi26/bookverse-helm.git'
    - 'https://github.com/Rodi26/bookverse-demo-assets.git'
  destinations:
    - namespace: 'bookverse-prod'
      server: 'https://kubernetes.default.svc'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

